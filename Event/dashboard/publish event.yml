info:
  name: publish event
  type: http
  seq: 3

http:
  method: POST
  url: "{{baseUrl}}/dashboard/events/69f51871c27668f00b9db24f/publish"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
