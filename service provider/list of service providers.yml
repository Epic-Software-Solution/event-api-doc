info:
  name: list of service providers
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/service-providers  "
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
