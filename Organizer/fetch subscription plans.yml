info:
  name: fetch subscription plans
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseUrl}}/subscription-plans/active?targetAudience=service_provider"
  params:
    - name: targetAudience
      value: service_provider
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
