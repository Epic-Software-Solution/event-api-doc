info:
  name: follow
  type: http
  seq: 11

http:
  method: POST
  url: "{{baseUrl}}/organizers/69f8508dc9cf331b865970fe/follow"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
