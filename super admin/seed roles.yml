info:
  name: seed roles
  type: http
  seq: 3

http:
  method: POST
  url: "{{baseUrl}}/super-admins/seed/auth"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
