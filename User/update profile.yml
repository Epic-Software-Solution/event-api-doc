info:
  name: update profile
  type: http
  seq: 5

http:
  method: PATCH
  url: "{{baseUrl}}/user/me"
  body:
    type: json
    data: |-
      {
        "avatar": "https://example.com/my-photo.jpg"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
