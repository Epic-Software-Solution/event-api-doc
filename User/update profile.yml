info:
  name: update profile
  type: http
  seq: 5

http:
  method: PATCH
  url: "{{baseUrl}}/users/me"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
