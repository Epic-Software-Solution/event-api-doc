info:
  name: register
  type: http
  seq: 1

http:
  method: POST
  url: "{{baseUrl}}/auth/user/signup"
  body:
    type: json
    data: |-
      {
        "firstName": "Abebe",
        "lastName": "Kebede",
        "email": "abebe@example.com",
        "phone":"+251942049329",
        
        "password": "User1234!"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
